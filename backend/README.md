# Semaphore Detection FastAPI Backend

## Cara Jalanin di Localhost:

1. **Buat Virtual Env (Opsional tapi direkomendasikan):**

```bash (contoh)
python -m venv semaphore-env
semaphore-env\Scripts\activate

```

<!-- Runn -->

uvicorn main:app --reload
